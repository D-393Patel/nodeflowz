export const PAGINATION ={
    DEFAULT_PAGE:1,
    DEFAULT_PAGE_SIZE:5,
    MAX_PAGE_SIZE:100,
    MIN_PAGE_SIZE:5,
};

//https://localhost:3000/workflows?search=plain
//reset to 
//https://localhost:3000/workflows

//cached,typesafety,speed,maintainble code =power of combining(trpc(data access layer) + tenstack +nuks)